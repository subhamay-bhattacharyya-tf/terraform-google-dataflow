# Dataflow Job with Custom Machine Type

Demonstrates sizing Dataflow workers for CPU/memory-intensive pipelines using `n1-highmem-4` machines with a fixed initial worker count and autoscaling ceiling.

## Usage

```bash
terraform init -backend=false
terraform validate
```
