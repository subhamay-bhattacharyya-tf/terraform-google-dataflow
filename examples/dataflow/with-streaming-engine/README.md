# Dataflow Job with Streaming Engine

Demonstrates enabling the Dataflow Streaming Engine for lower latency and reduced resource usage in streaming pipelines.

## Usage

```bash
terraform init -backend=false
terraform validate
```
