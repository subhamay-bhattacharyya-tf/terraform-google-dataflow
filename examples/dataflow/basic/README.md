# Basic Dataflow Job

Demonstrates a minimal Dataflow job using all required fields and accepting defaults for every optional parameter.

## Usage

```bash
terraform init -backend=false
terraform validate
```
