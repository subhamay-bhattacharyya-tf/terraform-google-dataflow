# Dataflow Job with Custom Service Account

Demonstrates running Dataflow workers under a dedicated service account with fine-grained IAM permissions instead of the default Compute Engine service account.

## Usage

```bash
terraform init -backend=false
terraform validate
```
