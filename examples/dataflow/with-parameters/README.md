# Dataflow Job with Template Parameters

Demonstrates passing template-specific parameters to the Dataflow job using the `parameters` map.

## Usage

```bash
terraform init -backend=false
terraform validate
```
